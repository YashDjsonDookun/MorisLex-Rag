# MORISLEX-RAG — v1: plain Python, no LangChain
# Install torch first so Apple Silicon gets MPS-enabled build (sentence-transformers will use it)
torch>=2.0.0
streamlit>=1.28.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
watchdog>=3.0.0
pydantic>=2.0.0
loguru>=0.7.0
python-dotenv>=1.0.0
pyyaml>=6.0
# OpenAI-compatible client for Ollama / LM Studio
openai>=1.6.0,<2.0.0
# Pipeline worker service
fastapi>=0.100.0
uvicorn>=0.22.0
requests>=2.28.0

# Tests (same as Engine)
pytest>=8.0.0
