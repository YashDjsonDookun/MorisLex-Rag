apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Optional: patch resources for local (e.g. lower limits). Base values are tuned to avoid restarts during pipeline runs.
